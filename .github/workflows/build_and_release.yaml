name: Build & Release

# Sets the trigger to changing the VERSION file in the main branch
on:
  push:
    branches:
    - main
    paths:
    - 'VERSION'

# Allows the workflow to create release
permissions:
  contents: write

jobs:
  build_and_release:
    runs-on: windows-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Create Output Directory
      run: New-Item -ItemType Directory -Path .\output -Force | Out-Null

    - name: Compile DLL for COM Hijacking
      run: |
        cd src
        & gcc -shared main.c -o ..\output\apt28.dll exports.def -s -m64 -Wall -Wextra "-Wl,--subsystem,windows" "-Wl,--gc-sections" "-Wl,--strip-all" -lole32 -loleaut32 -luuid -lstrmiids -lwinmm -lpsapi -liphlpapi
        if ($LASTEXITCODE -ne 0) {
          Write-Error "Compilation failed"
          exit 1
        }
        cd ..

    - name: Read Version
      run: |
        $version = Get-Content VERSION
        Write-Output "VERSION=$version" >> $env:GITHUB_ENV

    - name: Verify Build Output
      run: |
        if (-not (Test-Path .\output\apt28.dll)) {
          Write-Error "Build output not found: .\output\apt28.dll"
          exit 1
        }
        Write-Output "Build successful: $(Get-Item .\output\apt28.dll | ForEach-Object { $_.Length } ) bytes"

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ env.VERSION }}
        name: ${{ github.event.repository.name }} ${{ env.VERSION }}
        make_latest: true
        draft: false
        prerelease: false
        fail_on_unmatched_files: true
        files: ./output/*
