version: '3.8'

services:
  c2_server:
    build: .
    ports:
      - "8080:8080"
    environment:
      - C2_HOST=0.0.0.0
      - C2_PORT=8080
      - DEBUG=False
    volumes:
      - ./logs:/app/logs
    container_name: apt28_c2_server
    restart: unless-stopped
