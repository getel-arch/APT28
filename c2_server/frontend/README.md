# APT28 C2 Server Frontend

This is the React-based frontend dashboard for the APT28 C2 Server.

## Development

```bash
npm install
npm start
```

## Building for Production

```bash
npm run build
```

The build will be created in the `build/` directory and served by the Flask backend.
